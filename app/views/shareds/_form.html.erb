<%= form_with(model: shared, local: true) do |form| %>
  <% if shared.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shared.errors.count, "error") %> prohibited this shared from being saved:</h2>

      <ul>
        <% shared.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
